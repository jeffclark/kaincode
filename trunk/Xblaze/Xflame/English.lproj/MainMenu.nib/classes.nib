{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {login = id; setStatus = id; }; 
            CLASS = XFController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                buddyListTableView = NSTableView; 
                passwordField = NSTextField; 
                statusField = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}