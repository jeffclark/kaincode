{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {indexiPod = id; }; 
            CLASS = SPController; 
            LANGUAGE = ObjC; 
            OUTLETS = {iPodsTableView = NSTableView; indexButton = NSButton; mainWindow = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}